[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci --legacy-peer-deps"]

[phases.build]
cmds = ["npm cache clean --force && npm run build"]

[start]
cmd = "npx serve -s dist -l ${PORT:-3000}"

# Force rebuild - cache bust v1.4.0 - 2026-03-27T09
