# 일상점검 체크리스트 UI 설계 (개정판)

## 📱 모바일 버전 - 기본 정보 자동 입력

### 1. 시작 화면 - 일상점검 안내 도움말

```
┌────────────────────────────────────────┐
│           일상점검                     │
│  M-2024-001 - 스마트폰 케이스 금형     │
├────────────────────────────────────────┤
│                                        │
│  ⓘ 일상점검 안내                       │
│                                        │
│  매일 작업 시작 전 금형 상태를 점검하여│
│  안전한 생산을 보장합니다.             │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ ✓ 점검 주기                      │  │
│  │ • 매일 작업 시작 전 필수 점검    │  │
│  │ • 작업 중 이상 발견 시 즉시 점검 │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ ✓ 점검 항목                      │  │
│  │ • 금형 외관 상태                 │  │
│  │ • 게이트/런너 상태               │  │
│  │ • 슬라이드 작동                  │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ ✓ 누적타수 관리                  │  │
│  │ • 금형에 표시된 현재 누적타수를  │  │
│  │   확실하게 입력                  │  │
│  │ • 현상점검 시 자동으로 타수 이력 │  │
│  │   관리                           │  │
│  │ • 정기점검 및 세척/습합 주기 일정│  │
│  │   에 활용                        │  │
│  └──────────────────────────────────┘  │
│                                        │
│  💡 중요: 이상 발견 시 즉시 작업을     │
│     중단하고 관리자에게 보고해주세요.  │
│                                        │
│            [시작하기]                  │
│                                        │
└────────────────────────────────────────┘
```

### 2. 기본 금형 정보 화면 (자동 입력 + 필수 입력)

```
┌────────────────────────────────────────┐
│  ← 일상점검                            │
├────────────────────────────────────────┤
│                                        │
│  📍 금형 위치                          │
│  ┌──────────────────────────────────┐  │
│  │        A구역-01                  │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                        │
│  ⓘ 기본 금형 정보 (자동 입력)          │
│                                        │
│  ┌──────────────────────────────────┐  │
│  │ 품번                             │  │
│  │ M-2024-001                       │  │
│  │                                  │  │
│  │ 품명                             │  │
│  │ 스마트폰 케이스 금형             │  │
│  │                                  │  │
│  │ 현재 위치                        │  │
│  │ A구역-01                         │  │
│  │                                  │  │
│  │ 누적 쇼트수                      │  │
│  │ 9,500 회 (일상점검 데이터 반영)  │  │
│  │                                  │  │
│  │ 신규 입력                        │  │
│  │ 2025. 11. 20. 오후 01:09:15      │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                        │
│  📍 GPS 위치 * (필수)                  │
│  ┌──────────────────────────────────┐  │
│  │ ⚠️ 위치 정보 요청 시간이 초과되었 │  │
│  │ 습니다.                          │  │
│  │                                  │  │
│  │          [다시 시도]             │  │
│  └──────────────────────────────────┘  │
│                                        │
│  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  │
│                                        │
│  🔢 현재 누적타수 * (필수)             │
│  ┌──────────────────────────────────┐  │
│  │                                  │  │
│  │  [    9500    ]                  │  │
│  │                                  │  │
│  │  이전: 9,500회 (자동 표시)       │  │
│  │  현재 타수를 입력하세요          │  │
│  └──────────────────────────────────┘  │
│                                        │
│  💡 GPS 위치와 누적타수는 필수 입력    │
│     항목입니다                         │
│                                        │
│  [다음 단계] (GPS + 타수 입력 후 활성화)│
│                                        │
└────────────────────────────────────────┘
```

---

## 📊 자동 입력 항목

| 항목 | 데이터 소스 | 표시 방식 |
|------|------------|----------|
| **품번** | QR 스캔 데이터 | M-2024-001 |
| **품명** | 금형 마스터 DB | 스마트폰 케이스 금형 |
| **현재 위치** | 금형 마스터 DB | A구역-01 |
| **누적 쇼트수** | 마지막 점검 데이터 | 9,500 회 |
| **신규 입력 시간** | 시스템 시간 | 2025. 11. 20. 오후 01:09:15 |

---

## ✅ 필수 입력 항목

### 1. GPS 위치 (필수)

```javascript
// GPS 위치 자동 획득
async function getGPSLocation() {
  try {
    const position = await new Promise((resolve, reject) => {
      navigator.geolocation.getCurrentPosition(resolve, reject, {
        enableHighAccuracy: true,
        timeout: 10000,
        maximumAge: 0
      });
    });
    
    return {
      latitude: position.coords.latitude,
      longitude: position.coords.longitude,
      accuracy: position.coords.accuracy,
      timestamp: new Date()
    };
  } catch (error) {
    // GPS 획득 실패 시 재시도 유도
    showGPSError(error);
    return null;
  }
}

// GPS 검증
function validateGPSLocation(gps, expectedLocation) {
  const distance = calculateDistance(
    gps.latitude, 
    gps.longitude,
    expectedLocation.latitude,
    expectedLocation.longitude
  );
  
  // 100m 이내인지 확인
  if (distance > 100) {
    showWarning('현재 위치가 금형 보관 위치와 다릅니다.');
  }
  
  return true;
}
```

### 2. 현재 누적타수 (필수)

```javascript
// 누적타수 입력 검증
function validateShotCount(currentCount, previousCount) {
  // 이전 타수보다 작으면 오류
  if (currentCount < previousCount) {
    showError('현재 타수는 이전 타수보다 작을 수 없습니다.');
    return false;
  }
  
  // 일일 생산량 계산
  const dailyProduction = currentCount - previousCount;
  
  // 비정상적으로 많은 생산량 경고
  if (dailyProduction > 10000) {
    showWarning(`일일 생산량이 ${dailyProduction}회로 매우 많습니다. 확인해주세요.`);
  }
  
  // 생산량이 0이면 경고
  if (dailyProduction === 0) {
    showWarning('생산량이 0입니다. 금형이 가동되지 않았나요?');
  }
  
  return true;
}
```

---

## 🎨 UI 상태 관리

### GPS 위치 상태

```
┌────────────────────────────────────────┐
│  📍 GPS 위치 * (필수)                  │
│  ┌──────────────────────────────────┐  │
│  │ 🔄 위치 정보를 가져오는 중...    │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│  📍 GPS 위치 * (필수)                  │
│  ┌──────────────────────────────────┐  │
│  │ ✅ 위치 확인됨                   │  │
│  │ 37.5665° N, 126.9780° E          │  │
│  │ 정확도: ±5m                      │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│  📍 GPS 위치 * (필수)                  │
│  ┌──────────────────────────────────┐  │
│  │ ⚠️ 위치 정보 요청 시간이 초과    │  │
│  │ 되었습니다.                      │  │
│  │          [다시 시도]             │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

### 누적타수 입력 상태

```
┌────────────────────────────────────────┐
│  🔢 현재 누적타수 * (필수)             │
│  ┌──────────────────────────────────┐  │
│  │  [    9500    ]                  │  │
│  │                                  │  │
│  │  이전: 9,500회                   │  │
│  │  ⚠️ 현재 타수를 입력하세요        │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│  🔢 현재 누적타수 * (필수)             │
│  ┌──────────────────────────────────┐  │
│  │  [   12000    ]                  │  │
│  │                                  │  │
│  │  이전: 9,500회                   │  │
│  │  ✅ 당일 생산: +2,500회           │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘

┌────────────────────────────────────────┐
│  🔢 현재 누적타수 * (필수)             │
│  ┌──────────────────────────────────┐  │
│  │  [    9000    ]                  │  │
│  │                                  │  │
│  │  이전: 9,500회                   │  │
│  │  ❌ 오류: 이전 타수보다 작습니다  │  │
│  └──────────────────────────────────┘  │
└────────────────────────────────────────┘
```

---

## 💻 PC 버전 - 기본 정보 표시

```
┌──────────────────────────────────────────────────────────────────────┐
│  일상점검 상세 - M-2024-001                    [수정] [인쇄] [삭제]  │
├──────────────────────────────────────────────────────────────────────┤
│  ⓘ 기본 금형 정보 (자동 입력)                                        │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │ 품번          │ M-2024-001                                     │  │
│  │ 품명          │ 스마트폰 케이스 금형                           │  │
│  │ 현재 위치     │ A구역-01                                       │  │
│  │ 이전 누적타수 │ 9,500 회 (2024-11-19 17:00 기준)              │  │
│  │ 점검 시작     │ 2025-11-20 13:09:15                           │  │
│  └────────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ✅ 필수 입력 항목                                                   │
│  ┌────────────────────────────────────────────────────────────────┐  │
│  │ GPS 위치 *    │ ✅ 37.5665° N, 126.9780° E (정확도: ±5m)      │  │
│  │ 현재 누적타수*│ ✅ 12,000 회 (당일 생산: +2,500회)            │  │
│  └────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 📊 데이터베이스 스키마 (업데이트)

```sql
CREATE TABLE daily_checks (
  id SERIAL PRIMARY KEY,
  check_id VARCHAR(50) UNIQUE,
  mold_id INTEGER REFERENCES molds(id),
  
  -- 자동 입력 정보
  part_number VARCHAR(50),              -- 품번 (QR 스캔에서)
  part_name VARCHAR(200),               -- 품명 (금형 마스터에서)
  current_location VARCHAR(100),        -- 현재 위치 (금형 마스터에서)
  previous_shot_count INTEGER,          -- 마지막 점검에서
  check_start_time TIMESTAMP,           -- 시스템 시간
  
  -- 필수 입력 정보
  gps_latitude DECIMAL(10, 8) NOT NULL, -- GPS 위도 (필수)
  gps_longitude DECIMAL(11, 8) NOT NULL,-- GPS 경도 (필수)
  gps_accuracy DECIMAL(10, 2),          -- GPS 정확도
  gps_timestamp TIMESTAMP,              -- GPS 획득 시간
  current_shot_count INTEGER NOT NULL,  -- 현재 누적타수 (필수)
  daily_production INTEGER,             -- 당일 생산량 (자동 계산)
  
  -- 점검 항목들...
  appearance_status VARCHAR(20),
  -- ... (나머지 항목들)
  
  checked_by INTEGER REFERENCES users(id),
  created_at TIMESTAMP DEFAULT NOW(),
  
  -- 제약 조건
  CONSTRAINT check_shot_count CHECK (current_shot_count >= previous_shot_count)
);

-- GPS 위치 인덱스
CREATE INDEX idx_daily_checks_gps ON daily_checks(gps_latitude, gps_longitude);
```

---

## 🔄 자동 입력 로직

```javascript
// 일상점검 시작 시 자동 입력
async function startDailyCheck(qrData) {
  // 1. QR 데이터에서 금형 ID 추출
  const moldId = qrData.mold_id;
  
  // 2. 금형 마스터 정보 조회
  const moldInfo = await fetchMoldInfo(moldId);
  
  // 3. 마지막 점검 데이터 조회
  const lastCheck = await fetchLastDailyCheck(moldId);
  
  // 4. 자동 입력 데이터 구성
  const autoFilledData = {
    part_number: moldInfo.part_number,      // 품번
    part_name: moldInfo.part_name,          // 품명
    current_location: moldInfo.current_location,
    previous_shot_count: lastCheck?.current_shot_count || 0,
    check_start_time: new Date()
  };
  
  // 5. GPS 위치 자동 획득 시도
  const gpsLocation = await getGPSLocation();
  
  return {
    ...autoFilledData,
    gps_location: gpsLocation,
    requires_shot_count_input: true
  };
}
```

---

## ✅ 검증 규칙

### 1. GPS 위치 검증
- GPS 정보 필수 획득
- 정확도 50m 이내 권장
- 타임아웃: 10초
- 실패 시 재시도 가능

### 2. 누적타수 검증
- 이전 타수 이상 입력 필수
- 일일 생산량 자동 계산
- 비정상 생산량 경고 (10,000회 초과)
- 0 생산량 경고

### 3. 다음 단계 진행 조건
- GPS 위치 확인 완료
- 누적타수 입력 완료
- 검증 통과

이제 사진과 같이 기본 금형 정보가 자동으로 입력되고, GPS 위치와 현재 누적타수가 필수 입력 항목으로 구성되었습니다!
